# ==============================================================================
#  제주도 농업용 지하수 분석 대시보드 — 개발 전용 종속성
# ------------------------------------------------------------------------------
#  설치 방법 (VS Code 터미널, 운영 환경에서는 불필요):
#      pip install -r requirements.txt
#      pip install -r requirements-dev.txt
#
#  목적: 운영(streamlit run)에는 필요 없지만 개발·테스트 시 필요한 도구.
#       requirements.txt 의 운영 종속성을 가볍게 유지하기 위해 분리.
# ==============================================================================

# --- 테스트 ---
# pytest: tests/ 의 parametrize·smoke 테스트 일괄 실행.
#   사용: python -m pytest tests/
# 권장: 새 PR/큰 변경 후 회귀 가드로 일괄 실행. 메모리 규칙
#   "큰 모듈 분리 시 런타임 import 검증 필수" 정책 자동화.
pytest>=8.0.0
